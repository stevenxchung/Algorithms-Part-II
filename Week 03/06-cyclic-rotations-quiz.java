// Cyclic rotations
// Pseudo-code

public class CyclicRotations {
  // Helper function to determine if s and t are the same length
  private boolean isSameLength(String s, String t) {}

  // Helper function to determine if s consists of a suffix t followed by a prefix of t
  private boolean checkSuffixAndPrefix(String s, String t) {}

  // Helper function to determine if s and t are cyclic rotations of one another
  private boolean isCyclicRotation(String s, String t) {}

  // Determine if cyclic rotations exists for a pair of distinct strings in the array
  public boolean CyclicRotations(String[] n) {}

  // Tests
  public static void main(String[] args) {}
}
